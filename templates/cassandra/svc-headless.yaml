apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ template "cassandra.fullname" . }}
  name: {{ template "cassandra.fullname" . }}
spec:
  clusterIP: None
  ports:
    - port: 9042
  selector:
    app: {{ template "cassandra.fullname" . }}